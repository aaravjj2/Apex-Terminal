# Development environment variables
# Backend runs on :8000, frontend dev on :5100
# Empty VITE_API_URL = use relative paths → Vite proxy routes to backend
VITE_API_URL=
# Override locally if port 8000 is occupied (e.g. APEX_BACKEND_PORT=8001)
# APEX_BACKEND_PORT=8001
