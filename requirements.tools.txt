# Apex Terminal — Tools / Test Requirements
# Install with: pip install -r requirements.tools.txt

# Async test support
pytest-asyncio>=0.23.0

# HTTP testing without running a full server
httpx>=0.27.0

# WebSocket testing (for autopilot WS endpoints)
websocket-client>=1.7.0

# JSON schema validation
jsonschema>=4.22.0

# ES Python client (optional, used in scripts)
elasticsearch>=8.12.0

# Dotenv for loading keys.env in scripts
python-dotenv>=1.0.0

# Additional test utilities
pytest>=8.0.0
pytest-timeout>=2.3.0
